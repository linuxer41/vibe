# Backend: "rust" -> port 3001, "node" -> port 3000
VITE_BACKEND=node

# Override URLs manually if needed:
# VITE_SOCKET_URL=http://localhost:3000
# VITE_API_URL=http://localhost:3000

# Storage server URL (también configurable en Ajustes > Almacenamiento)
# VITE_STORAGE_URL=http://localhost:5000
